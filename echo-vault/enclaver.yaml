version: v1
name: echo-vault

# Application entrypoint
target: echo-vault:latest

sources:
  app: .

defaults:
  cpu_count: 2
  memory_mb: 2048

ingress:
  - listen_port: 8000
  - listen_port: 18001

egress:
  allow:
    - "**"

api:
  listen_port: 18000
