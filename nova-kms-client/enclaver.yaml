version: v1
name: "nova-kms-client"
target: "nitro-enclave"

sources:
  - Dockerfile

ingress:
  - listen_port: 8000
  - listen_port: 18001

egress:
  allow:
    - 0.0.0.0/0:443
    - 0.0.0.0/0:80

env:
  - IN_ENCLAVE
