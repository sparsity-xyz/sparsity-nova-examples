version: v1
name: hello-world-tee
target: hello-world-tee:latest

sources:
  app: .

defaults:
  memory_mb: 1024

ingress:
  - listen_port: 8000

egress:
  allow:
    - "**"

api:
  listen_port: 18000

aux_api:
  listen_port: 18001
